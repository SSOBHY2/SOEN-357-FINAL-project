// Configuration file - API key loaded from .env
// Note: In a browser extension, we can't directly read .env files at runtime
// This file should be updated manually or via a build process
// For production, consider using Chrome's storage API or a build tool

const CONFIG = {
    BACKEND_URL: 'http://localhost:8000'
};
